{% extends '@baihu_baihu/layout/profile-base.twig' %}

{% block gzo_main_content %}

	{% EVENT memberlist_view_content_prepend %}

	<div class="block profile-block">
		{# <div class="bar block-bar">
			<span class="bar-item bar-info">
				<h3>{{ lang('USER_FORUM') }}</h3>
			</span>
		</div> #}

		<div class="profile-content">Under Construction...</div>

			{# <h5 class="profile-meta-5">{{ lang('USER_FORUM') }}</h5> #}
				{# {% if S_GROUP_OPTIONS %}
					{% import '@baihu_baihu/macros/forms.twig' as form %}

					<dt>{{ lang('USERGROUPS') ~ lang('COLON') }}</dt>
					<dd>
						{{ form.select('g', S_GROUP_CURRENT, S_GROUP_OPTIONS, true) }} <input type="submit" name="submit" value="{{ lang('GO') }}" class="btn" />
					</dd>
				{% endif %} #}

	</div>

	{% if SIGNATURE %}
		<div class="block profile-block">
			<div class="bar block-bar">
				<span class="bar-item bar-info">
					<h3>{{ lang('SIGNATURE') }}</h3>
				</span>
			</div>

			<div class="profile-content">{{ SIGNATURE }}</div>

		</div>
	{% endif %}

	{% EVENT memberlist_view_content_append %}

{% endblock %}
